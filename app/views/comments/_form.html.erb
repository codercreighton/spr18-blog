<%= form_with(model: comment, local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
   
    <%= form.text_field :author, placeholder: "Who are you?", class: 'form-control' %>
  </div>

  <div class="form-group">
    
    <%= form.text_area :comment_entry, placeholder: "What's on your mind?", class: 'form-control' %>
  </div>

  <div class="form-group">
      <%= form.hidden_field :post_id, value: @post.id, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.submit "Don't be shy, tell the world!", class: "form-control" %>
  </div>
<% end %>
